name: beacon
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+4

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_beacon: ^0.5.1

  flutter_secure_storage: ^10.3.1
  http: ^1.6.0
  uuid: ^4.5.3
  url_launcher: ^6.3.1

  # Faz 6 - mobil iskelet: durum yonetimi + yonlendirme + gercek cihaz/surum bilgisi.
  flutter_riverpod: ^3.4.2
  go_router: ^17.4.0
  package_info_plus: ^10.2.1
  device_info_plus: ^13.2.0

  # Faz 7 - mobil ekranlar. Ikisi de pub.dev'den DOGRULANDI (2026-08-09):
  # - flutter_markdown_plus: resmi `flutter_markdown` paketi DURDURULDU
  #   ("discontinued", yayincisinin kendisi bu paketi resmi devam projesi
  #   olarak gosteriyor); bu surum 1 aydan az once yayinlandi, SDK kisiti
  #   projeyle uyumlu. Genel Bilgi bolumlerinin Markdown govdesini render
  #   etmek icin.
  # - extended_image: `cached_network_image` (en bilinen alternatif)
  #   GitHub'da ~2 yildir commit almiyor (331 acik issue) - fiilen bakimsiz.
  #   extended_image dogrulanmis yayinci (fluttercandies), 1 aydan az once
  #   yayinlandi, azami pub points, agdan gorseli yerelde onbellekleyip
  #   `cache: true` ile tekrar indirmeden gosteriyor - kapak/sponsor/
  #   konusmaci gorselleri icin.
  flutter_markdown_plus: ^1.0.12
  extended_image: ^10.1.0

  # Program/icerik onbellegi icin kalici JSON dosyasi yazmak amaciyla -
  # uygulamanin kendi belge dizinine erisim saglar (bkz. Faz 7 talimati §1,
  # `flutter_secure_storage` kucuk sirlar icindir, bu amac icin degil).
  # Flutter ekibinin resmi eklentisi, halihazirda transitive bagimlilik
  # olarak vardi, artik dogrudan kullaniliyor.
  path_provider: ^2.1.6

  # Faz 8 - kalici cevrimdisi gozlem kuyrugu. Ikisi de pub.dev'den DOGRULANDI
  # (2026-08-10): sqflite 2.4.3 (2 ay once yayinlandi), path 1.9.1 (projede
  # zaten transitive bagimlilik olarak vardi - `path_provider` uzerinden -
  # artik dogrudan kullaniliyor, sadece pubspec'te ACIKCA belirtiliyor).
  sqflite: ^2.4.3
  path: ^1.9.1

  # Faz 9 - push bildirimleri (FCM). Ikisi de pub.dev'den DOGRULANDI
  # (2026-08-11): firebase_core 4.13.0, firebase_messaging 16.5.0 (ikisi de
  # 7 gun once yayinlandi). Ek bir yerel-bildirim paketi (ör.
  # flutter_local_notifications) EKLENMEDI: iOS'ta onplandayken sistem
  # bildirim baloncugunu gostermek icin firebase_messaging'in kendi
  # `setForegroundNotificationPresentationOptions` API'si yeterli - ayrica
  # bir pakete GEREK YOK (bkz. push_notification_lifecycle_provider.dart).
  firebase_core: ^4.13.0
  firebase_messaging: ^16.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Faz 8 - kuyruk testlerinde gercek SQLite semantigini (UNIQUE kisiti, ORDER
  # BY, DELETE) fiziksel cihaz/emulator OLMADAN dogrulamak icin - sqflite'in
  # resmi paketi, masaustunde saf FFI ile calisan bir SQLite motoru saglar.
  # pub.dev'den DOGRULANDI (2026-08-10): 2.4.2, 60 gun once yayinlandi.
  sqflite_common_ffi: ^2.4.2
  # Faz 7 - XCUITest'in Flutter icin yanlis arac oldugu (widget agacini
  # gormuyor, yalnizca Semantics ile ACIKCA verilmis identifier'lari
  # gorebiliyor) canli denemeyle dogrulandiktan sonra bunun yerine
  # kullanilan resmi Flutter paketi - widget agacina DOGRUDAN erisir, ek
  # bir Xcode target'i gerektirmez.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
